apiVersion: v1
kind: Service
metadata:
  name: wazuh-llm
  labels:
    app.kubernetes.io/name: wazuh-llm
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: wazuh-llm
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
